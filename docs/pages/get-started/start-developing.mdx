import React from 'react';
import { View, Text, ScrollView, TouchableOpacity, Image } from 'react-native';

export default function App() {
  return (
    <ScrollView style={{ flex: 1, padding: 16, backgroundColor: '#ffffff' }}>
      {/* Pantalla Resumen del día */}
      <Text style={{ fontSize: 24, fontWeight: 'bold', marginBottom: 16 }}>Resumen del día</Text>
      <View style={{ padding: 12, backgroundColor: '#007bff', borderRadius: 8, marginBottom: 8 }}>
        <Text style={{ color: 'white', fontSize: 18 }}>Ingresos: S/800.00</Text>
      </View>
      <View style={{ padding: 12, backgroundColor: '#cce5ff', borderRadius: 8, marginBottom: 16 }}>
        <Text style={{ color: '#333', fontSize: 18 }}>Gastos: S/320.00</Text>
      </View>

      {/* Últimos movimientos */}
      <Text style={{ fontSize: 20, fontWeight: '600', marginBottom: 8 }}>Últimos movimientos</Text>
      {[
        { title: 'Venta de pescado', date: '23 abr.' },
        { title: 'Compra de hielo', date: '22 abr.' },
        { title: 'Venta a restaurante', date: '21 abr.' }
      ].map((item, index) => (
        <View key={index} style={{ padding: 12, backgroundColor: '#f8f9fa', borderRadius: 8, marginBottom: 8 }}>
          <Text>{item.title}</Text>
          <Text style={{ fontSize: 12, color: 'gray' }}>{item.date}</Text>
        </View>
      ))}

      {/* Vencimientos de pagos */}
      <Text style={{ fontSize: 20, fontWeight: '600', marginVertical: 16 }}>Vencimientos de pagos</Text>
      {['1 de julio 2024', '15 de julio 2024', '31 de julio 2024'].map((date, index) => (
        <View key={index} style={{ padding: 12, backgroundColor: '#f8d7da', borderRadius: 8, marginBottom: 8 }}>
          <Text>Pago - {date}</Text>
        </View>
      ))}
    </ScrollView>
  );
}
